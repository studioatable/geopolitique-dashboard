# Dépendances Python — geopolitique-dashboard
# Installation : pip install -r requirements.txt
# Versions épinglées pour reproductibilité (mises à jour passées au crible audit avant bump).

requests>=2.31,<3.0
feedparser>=6.0,<7.0
openpyxl>=3.1,<4.0
